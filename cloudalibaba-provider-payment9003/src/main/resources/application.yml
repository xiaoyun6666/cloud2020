server:
  port: 9003

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置nacos服务注册中心地址
        #server-addr: 192.168.115.128:1111 #配置nacos集群地址

management:
  endpoints:
    web:
      exposure:
        include: '*'
